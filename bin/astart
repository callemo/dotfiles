#!/bin/sh
for i in plumber fontsrv
do
  if pgrep "$i"
  then
    continue
  fi
  "$i" &
done

set -a

EDITOR=E
ENV=$HOME/dotfiles/acme.sh
PAGER=nobs
SHELL=/bin/dash
acmeshell="$SHELL"
home="$HOME"
user="$USER"

exec 9 acme -a -W 1200x600 "$@"
