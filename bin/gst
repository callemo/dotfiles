#!/bin/sh
# gst: git status against the latest remote refs.

if git rev-parse --abbrev-ref '@{u}' >/dev/null 2>&1; then
	git fetch --quiet --prune --no-write-fetch-head 2>/dev/null
fi

exec git status "$@"
